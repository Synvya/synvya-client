# AWS Lambda Environment Variables
# Copy this file to set up your deployment environment

# Required for deployment
export ZAPRITE_API_KEY="your_zaprite_api_key_here"

# Optional - These have defaults but can be overridden
# export AWS_REGION="us-east-1"
# export LAMBDA_STACK_NAME="synvya-lambda-infrastructure"
# export LAMBDA_S3_BUCKET="synvya-subscriptions-prod"

# For GitHub Actions, add these as repository secrets:
# ZAPRITE_API_KEY (your Zaprite API key)
# AWS_DEPLOY_ROLE_ARN (already configured in your repo) 