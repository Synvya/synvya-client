# Production Environment Configuration for Synvya Frontend
# Copy these variables to your .env.production file
# These variables will be baked into the build at compile time

# Nostr Relay Configuration
VITE_NOSTR_RELAYS="wss://relay.primal.net,wss://relay.damus.io,wss://nos.lol,wss://relay.snort.social"

# File Upload Service
VITE_BLOSSOM_SERVER="https://blossom.band"

# Application Configuration
VITE_APP_NAME="Synvya"
VITE_APP_VERSION="1.0.0"

# Security Configuration
VITE_ENVIRONMENT="production"

# Optional: Analytics (if you want to add analytics later)
# VITE_ANALYTICS_ID=""

# Optional: Error Reporting
# VITE_SENTRY_DSN="" 